import React from "react";
import { Route } from "react-router-dom";
import Splash from './splash/splash'

const App = () => (
  <div>
    <header>
    </header>
      <Splash />
  </div>
);

export default App;